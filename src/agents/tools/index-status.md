Inspect the active index for a local path or Git repository.

Use this to discover the selected source, whether it is cached in memory, how
many files and chunks are indexed, which languages are covered, and which tools
are available before searching.
